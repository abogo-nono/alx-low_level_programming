/**
 * _abs - a function that computes the absolute value of an integer
 * @n: number
 * Return: -(n) if the number is less than 0 and n else
 */
int _abs(int n)
{
	if (n < 0)
		return (-(n));

	return (n);
}
